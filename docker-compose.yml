version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: ./infrastructure/docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=development
    volumes:
      - ./logs:/app/logs

